import 'package:flutter/material.dart';

class DirectMethod extends StatelessWidget {
  const DirectMethod({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
