import 'package:flutter/material.dart';

class NeTexPage extends StatelessWidget {
  const NeTexPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
